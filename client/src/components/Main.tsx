import { Stack } from '@chakra-ui/core'

export const Main = (props) => (
  <Stack
    position="relative"
    spacing="1.5rem"
    width="100%"
    maxWidth="58rem"
    pt="8rem"
    px="1rem"
    {...props}
  />
)
